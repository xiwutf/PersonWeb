/**
 * Naive UI 插件
 * 
 * 注意：Naive UI 支持按需引入，不需要全局注册
 * 在组件中直接 import 需要的组件即可使用
 * 
 * 示例：
 * import { NButton, NInput } from 'naive-ui'
 */
export default defineNuxtPlugin(() => {
  // Naive UI 不需要全局注册，按需引入即可
  // 如果需要全局主题配置，可以在 layouts/admin.vue 中使用 NConfigProvider
})

