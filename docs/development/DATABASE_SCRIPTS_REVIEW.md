# 数据库脚本文件审查报告

生成时间：2024-12-XX

## 📋 审查范围

检查 `database/` 目录下的所有 SQL 脚本文件，确定哪些是必需的，哪些可以删除。

---

## ✅ 必需保留的脚本文件

### 1. 核心表结构脚本

#### `all_tables.sql` ✅ **必需**
- **用途**：包含所有核心表的基础结构
- **状态**：必需保留
- **说明**：这是数据库的基础结构文件，包含 Articles、Categories、Tags、Users 等核心表

#### `projects_table.sql` ✅ **必需**
- **用途**：项目表结构
- **状态**：必需保留
- **说明**：项目功能的核心表

#### `side_project_tables.sql` ✅ **必需**
- **用途**：副业项目表结构
- **状态**：必需保留
- **说明**：副业管理功能的核心表，最近新增

#### `knowledge_base_tables.sql` ✅ **必需**
- **用途**：知识库表结构
- **状态**：必需保留
- **说明**：知识库功能的核心表

#### `changelog_roadmap_tables.sql` ✅ **必需**
- **用途**：更新日志和路线图表结构
- **状态**：必需保留
- **说明**：首页展示更新日志和路线图功能

#### `module_system_tables.sql` ✅ **必需**
- **用途**：模块系统表结构
- **状态**：必需保留
- **说明**：模块化架构的核心表

#### `toolbox_marketplace_tables.sql` ✅ **必需**
- **用途**：工具箱市场表结构
- **状态**：必需保留
- **说明**：工具功能的核心表

#### `task_tables.sql` ✅ **必需**
- **用途**：任务管理表结构
- **状态**：必需保留
- **说明**：任务和目标管理功能

#### `investment_tables.sql` ✅ **必需**
- **用途**：投资管理表结构
- **状态**：必需保留
- **说明**：投资功能的核心表

#### `skill_tree_tables.sql` ✅ **必需**
- **用途**：技能树表结构
- **状态**：必需保留
- **说明**：技能管理功能

#### `time_capsule_table.sql` ✅ **必需**
- **用途**：时间胶囊表结构
- **状态**：必需保留
- **说明**：时间胶囊功能

#### `visitor_*_tables.sql` ✅ **必需**
- **文件**：
  - `visitor_gamification_tables.sql`
  - `visitor_interaction_tables.sql`
- **用途**：访客互动和游戏化表结构
- **状态**：必需保留
- **说明**：访客功能的核心表

#### `commercial_features_tables.sql` ✅ **必需**
- **用途**：商业化功能表结构
- **状态**：必需保留
- **说明**：主题商店、API 等功能

#### `dashboard_metric_tables.sql` ✅ **必需**
- **用途**：仪表盘指标表结构
- **状态**：必需保留
- **说明**：仪表盘功能

#### `document_agent_tables.sql` ✅ **必需**
- **用途**：文档知识管家 Agent 表结构
- **状态**：必需保留
- **说明**：文档功能的核心表

#### `style_management_tables.sql` ✅ **必需**
- **用途**：样式管理表结构
- **状态**：必需保留
- **说明**：样式管理功能

#### `theme_switcher_tables.sql` ✅ **必需**
- **用途**：主题切换表结构
- **状态**：必需保留
- **说明**：主题功能

#### `home_style_tables.sql` ✅ **必需**
- **用途**：首页样式表结构
- **状态**：必需保留
- **说明**：首页样式管理

#### `admin_style_tables.sql` ✅ **必需**
- **用途**：后台样式表结构
- **状态**：必需保留
- **说明**：后台样式管理

#### `frontend_page_styles_tables.sql` ✅ **必需**
- **用途**：前端页面样式表结构
- **状态**：必需保留
- **说明**：前端样式管理

### 2. 辅助表脚本

#### `error_log_table.sql` ✅ **建议保留**
- **用途**：错误日志表
- **状态**：建议保留（用于生产环境错误追踪）

#### `friend_links_table.sql` ✅ **建议保留**
- **用途**：友情链接表
- **状态**：建议保留（如果使用友情链接功能）

#### `article_version_history.sql` ✅ **建议保留**
- **用途**：文章版本历史表
- **状态**：建议保留（如果使用版本历史功能）

#### `fulltext_index.sql` ✅ **建议保留**
- **用途**：全文索引
- **状态**：建议保留（提升搜索性能）

### 3. 数据修复/维护脚本

#### `side_project_add_income_type.sql` ✅ **保留**
- **用途**：为副业项目表添加收入类型字段
- **状态**：保留（用于数据库迁移）

#### `projects_add_view_count.sql` ✅ **保留**
- **用途**：为项目表添加浏览量字段
- **状态**：保留（用于数据库迁移）

#### `skill_table_update.sql` / `skill_table_update_simple.sql` ✅ **保留**
- **用途**：技能表更新脚本
- **状态**：保留（用于数据库迁移）

#### `style_management_*.sql` ✅ **保留**
- **文件**：
  - `style_management_fix.sql`
  - `style_management_init_defaults.sql`
  - `style_management_tables_alter.sql`
- **用途**：样式管理表的修复和更新
- **状态**：保留（用于数据库迁移）

---

## ⚠️ 可以删除的脚本文件

### 1. 重复/临时脚本

#### `check_duplicate_projects.sql` ⚠️ **可删除**
- **用途**：检查重复项目（一次性脚本）
- **状态**：可删除
- **说明**：这是临时检查脚本，执行后可以删除

#### `remove_duplicate_projects.sql` ⚠️ **可删除**
- **用途**：删除重复项目（一次性脚本）
- **状态**：可删除
- **说明**：这是临时清理脚本，执行后可以删除

#### `remove_duplicate_data.sql` ⚠️ **可删除**
- **用途**：删除重复数据（一次性脚本）
- **状态**：可删除
- **说明**：这是临时清理脚本，执行后可以删除

#### `remove_foreign_keys_tool_collection.sql` ⚠️ **可删除**
- **用途**：移除工具集合表的外键约束（一次性脚本）
- **状态**：可删除
- **说明**：这是临时修复脚本，执行后可以删除

### 2. 示例数据脚本（可选）

#### `sample_data.sql` / `sample_data_insert.sql` ⚠️ **可选删除**
- **用途**：示例数据插入
- **状态**：可选删除
- **说明**：
  - 如果已经导入数据，可以删除
  - 如果需要用于测试环境，建议保留
  - 建议：保留 `sample_data.sql`，删除 `sample_data_insert.sql`（如果内容重复）

#### `complete_content_data.sql` ⚠️ **可选删除**
- **用途**：完整内容数据
- **状态**：可选删除
- **说明**：
  - 如果已经导入数据，可以删除
  - 如果需要用于测试环境，建议保留

#### `enrich_content_data.sql` ⚠️ **可选删除**
- **用途**：丰富内容数据
- **状态**：可选删除
- **说明**：
  - 如果已经导入数据，可以删除
  - 如果需要用于测试环境，建议保留

#### `tool_sample_data.sql` / `tool_collection_sample_data.sql` ⚠️ **可选删除**
- **用途**：工具示例数据
- **状态**：可选删除
- **说明**：
  - 如果已经导入数据，可以删除
  - 如果需要用于测试环境，建议保留

#### `visitor_sample_data.sql` ⚠️ **可选删除**
- **用途**：访客示例数据
- **状态**：可选删除
- **说明**：
  - 如果已经导入数据，可以删除
  - 如果需要用于测试环境，建议保留

### 3. 文档文件

#### `README.md` / `README_*.md` ✅ **保留**
- **用途**：文档说明
- **状态**：必需保留
- **说明**：这些是重要的文档文件

#### `SETUP.md` ✅ **保留**
- **用途**：数据库设置指南
- **状态**：必需保留

#### `DESIGN_PRINCIPLES.md` ✅ **保留**
- **用途**：数据库设计原则
- **状态**：必需保留

---

## 📊 总结

### 必需保留的文件（约 25 个）
- 所有表结构脚本（`.sql` 文件，除了下面列出的可删除项）
- 所有文档文件（`.md` 文件）
- 数据库迁移脚本（`*_add_*.sql`, `*_update*.sql`, `*_fix.sql`, `*_alter.sql`）

### 可删除的文件（约 5-8 个）
1. `check_duplicate_projects.sql` - 临时检查脚本
2. `remove_duplicate_projects.sql` - 临时清理脚本
3. `remove_duplicate_data.sql` - 临时清理脚本
4. `remove_foreign_keys_tool_collection.sql` - 临时修复脚本
5. `sample_data_insert.sql` - 如果与 `sample_data.sql` 重复
6. 其他示例数据脚本（如果已经导入数据且不需要用于测试）

### 建议操作

1. **立即删除**：临时脚本（`check_*`, `remove_*`）
2. **可选删除**：示例数据脚本（如果已经导入数据）
3. **保留**：所有表结构脚本和文档文件

---

## 🔍 验证方法

执行以下 SQL 验证所有必需的表是否存在：

```sql
USE personal_site;
SHOW TABLES;
```

应该看到所有在 `AppDbContext.cs` 中定义的 `DbSet` 对应的表。

