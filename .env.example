# 前端环境变量配置模板
# 复制此文件为 .env 并填写实际值

# API 基础路径
# 开发环境：http://localhost:5234/api
# 生产环境：https://your-domain.com/api
NUXT_PUBLIC_API_BASE=http://localhost:5234/api

# 管理后台登录（Nitro /api/auth/login，必填）
# 用户名默认 admin，仅校验此密码
ADMIN_PASSWORD=your_admin_password
# 可选：JWT 签名密钥，默认复用 ADMIN_PASSWORD
# ADMIN_AUTH_SECRET=

# MindTrace 外部发布 Token 已迁到 .NET：
# backend/PersonalSite.Api/appsettings*.json → MindTrace:IngestToken
# 或环境变量 MindTrace__IngestToken / MINDTRACE_INGEST_TOKEN（按宿主映射）
# 不要再配置到 Nuxt。

# .NET 后端 JWT（与 backend/PersonalSite.Api appsettings Jwt 保持一致）
# 登录成功后会签发 backendToken，供后台调用 .NET API
# DOTNET_JWT_KEY=YourSuperSecretKeyHere_MustBeAtLeast32BytesLong
# DOTNET_JWT_ISSUER=PersonalSite.Api
# DOTNET_JWT_AUDIENCE=PersonalSite.Web
